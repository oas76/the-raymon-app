{
  "name": "the-raymon-app",
  "version": "1.0.0",
  "description": "A comprehensive authentication and profile management service for golfers",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "commit": "./scripts/commit-and-push.sh",
    "save": "./scripts/commit-and-push.sh",
    "git-status": "git status",
    "git-log": "git log --oneline -10",
    "vercel-build": "cd frontend && npm install && npm run build",
    "railway-build": "npm run install-all && cd frontend && npm run build"
  },
  "keywords": [
    "golf",
    "authentication",
    "oauth",
    "express",
    "react",
    "mongodb"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}